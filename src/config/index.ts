const config = {
  BASE_URL: import.meta.env.VITE_BASE_URL,
  GITHUB_PERSONAL_ACCESS_TOKEN: import.meta.env
    .VITE_GITHUB_PERSONAL_ACCESS_TOKEN,
  GITHUB_APP_ID: import.meta.env.VITE_GITHUB_APP_ID,
};

export default config;
