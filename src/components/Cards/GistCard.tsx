import { Card, CardContent } from "@mui/material";

export default function GistCard() {
  return (
    <Card elevation={2} sx={{ width: "30%" }}>
      <CardContent>Something</CardContent>
    </Card>
  );
}
